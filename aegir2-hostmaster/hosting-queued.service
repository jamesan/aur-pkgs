[Unit]
Description=Aegir queue daemon
After=local-fs.target network.target mysqld.service httpd.service

[Service]
Type=simple
ExecStart=/usr/bin/drush --quiet @hostmaster hosting-queued
User=aegir
Restart=always

[Install]
WantedBy=multi-user.target
